<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
        ContextLoaderListner에서 루트 웹 어플리케이션 컨텍스트를 생성하기 위한 설정 파일의 경로 지정
        - 보통 레거시들은 이렇게 XML 파일에 경로지정들을 한다
    -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/spring/root-context.xml</param-value>
    </context-param>
    
    <!--
        ContextLoaderListner
        - 웹 어플리케이션의 시작, 종료에 대한 이벤트 리스너
        - ServletContextListner를 구현한 클래스
        - 웹 어플리케이션 구동 시 루트 웹 어플리케이션 컨텍스트를 생성하고, 종료시에 소멸시킨다.
    -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>


    <!--
        DispatcherServlet
        - 모든 사용자의 요청을 받는 프론트 컨트롤러 서블릿
        - 이 서블릿마다 독립적인 웹 어플리케이션 컨텍스트를 가지고, 각각에 웹과 관련된 빈들이 생성된다.
    -->
    <servlet>
        <servlet-name>appServlet</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>

        <!--DispatcherServlet마다 독립적으로 생성될 WAC의 설정파일 경로-->
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/appServlet/servlet-context.xml</param-value>
        </init-param>

        <!-- 웹 어플리케이션이 시작될 때 1개의 DispatcherServlet을 미리 생성하고 시작함-->
        <load-on-startup>1</load-on-startup>
        
    </servlet>

    <servlet-mapping>
        <servlet-name>appServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

</web-app>
